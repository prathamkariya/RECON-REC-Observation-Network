# The dashboard image builds with dashboard/ as its context.
node_modules
.next
out
build

# Secrets — NEXT_PUBLIC_* values are passed as build args instead.
.env
.env.local
.env.*.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.git
.gitignore
.DS_Store
*.md
!README.md
