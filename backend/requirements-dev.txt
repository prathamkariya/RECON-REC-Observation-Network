# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r backend/requirements.txt -r backend/requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
pytest-cov>=5.0.0

# fastapi.testclient.TestClient is a thin wrapper over httpx.
httpx>=0.27.0

# The on-chain tests compile RECRegistry.sol with solc and deploy it to an
# in-process EVM, so they exercise web3_client's real code path with no
# Hardhat/Sepolia node running.
eth-tester[py-evm]>=0.12.0
py-solc-x>=2.0.0
