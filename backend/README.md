# Role 4: FastAPI Service & Dashboard API

Responsible for:
1. Exposing endpoints for ingesting, scoring, and querying certificates.
2. Integrating outputs from `ml/`, `graph_explain/`, and `ledger_cloud/`.
3. Serving mock or live endpoints to the `dashboard/`.
